replicaCount: 3

image:
  repository: brucewayne1ua/kubernetes-practice-nginx
  pullPolicy: IfNotPresent
  tag: "v1"

service:
  type: ClusterIP
  port: 80

persistence:
  enabled: true
  size: 1Gi

ingress:
  enabled: true
  host: nginx.local